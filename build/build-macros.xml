<project name="macros">
	<property name="build.project.dir" location="../build" />
	<property name="build.output.dir" location="bin" />
	
	<property name="this.project.dir" location="${basedir}" />
	<property name="this.project.output.dir" location="${build.output.dir}/${this.project.name}" />
	
	<macrodef name="clean-project">
		<attribute name="project" value="${this.project.name}" />
		<sequential>
			<delete dir="${build.output.dir}
		</sequential>
	</macrodef>
	
	<macrodef name="compile">
		<attribute name="srcdir" />
		<sequential>
			
		</sequential>
	</macrodef>
</project>